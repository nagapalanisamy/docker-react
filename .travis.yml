language: generic
sudo: required
services:
  - docker
before_install:
  - docker build -t nagapalanisamy/docker-react -f Dockerfile.dev .
script:
  - docker run -e CI=true nagapalanisamy/docker-react npm run test -- --coverage
